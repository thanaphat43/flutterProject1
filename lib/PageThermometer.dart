import 'package:flutter/material.dart';

class Mythermomerter extends StatelessWidget {
  Mythermomerter();

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      theme: ThemeData.dark(),
      home: Scaffold(),
    );
  }
}
